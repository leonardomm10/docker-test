server {
    listen 80 default_server;
    listen [::]:80 default_server;

    server_name _;

    location /basic_status {
        stub_status;
        allow 127.0.0.1;
        deny all;
    }

    location / {
        return 444;
    }
}
